import 'package:flutter/material.dart';

class SettingComp extends StatelessWidget {
  const SettingComp({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Text("SettingComp"));
  }
}
